import { Input,InputGroup, InputLeftElement, Stack, IconButton, HStack, Button, Heading } from '@chakra-ui/react'

export default function CreditMixForm(){
    const [creditType, setCreditUtils] = useState([
        "",""
    ]);
    function HandleChange(){
        let newCreditTyped
    }
    return (
        <>

        </>
    )
}